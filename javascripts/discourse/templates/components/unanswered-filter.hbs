{{#unless staffOnly}}
  {{#if shouldRender}}
    {{combo-box
      content=statuses
      value=queryStatus
      valueAttribute="value"
      onSelect=(action "changeStatus")
      class="topic-unanswered-filter-dropdown"
    }}
  {{/if}}
{{/unless}}
